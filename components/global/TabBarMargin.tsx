import { StyleSheet, View } from "react-native";

export function TabBarMargin() {
  return <View style={styles.tabBarMargin} />;
}

const styles = StyleSheet.create({
  tabBarMargin: {
    height: 110,
  },
});
